version: 2

seeds:
  - name: fx_rates_timeseries
    description: "Mock FX rates — USD base, daily 2020-2024, 5 currencies (EUR/CNY/JPY/GBP/CAD)"
    config:
      column_types:
        rate_date: date
        base_currency: string
        quote_currency: string
        exchange_rate: decimal(18, 6)
